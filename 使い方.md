# So-net 自動申込 RPA 使い方ガイド

## このツールでできること

kintone (app 178: So-net光 NUW) のレコード詳細画面で **「So-net 申込実行」ボタン** を押すと、Firefox が自動起動して So-net 申込フォームに自動入力します。確認画面で停止するので、内容を目視確認してから手動で「申込」ボタンを押します。

---

## 1. 初回セットアップ（5〜10分、1回だけ）

### 必要なもの
- Windows 10/11 PC（管理者権限は不要、ユーザー権限で OK）
- インターネット接続
- 配布された ZIP ファイル

### 手順

#### Step 1: ZIP を解凍
1. 配布された `sonet-rpa-distribution.zip` を **デスクトップ** に保存
2. ZIP を右クリック → **「すべて展開」**
3. 展開先はそのままで「展開」をクリック
4. 解凍後のフォルダ（`sonet-rpa-distribution`）を開く

#### Step 2: setup.bat を実行
1. フォルダ内の **`setup.bat`** をダブルクリック
2. 黒い画面（コマンドプロンプト）が開く
3. しばらく待つ（3〜5分）

#### Step 3: Python が未インストールの場合のみ
- 「Python が見つかりません」と表示された場合:
  1. 自動で Microsoft Store が開く
  2. Python 3.13 のページで **「入手」** ボタンをクリック
  3. インストール完了を待つ
  4. setup.bat をもう一度ダブルクリック

#### Step 4: 完了
- 「**セットアップ完了！**」と表示されたら準備 OK
- Enter キーで黒い画面を閉じる

---

## 2. 日常の使い方

### ボタンで実行

1. ブラウザで kintone を開く → **app 178 (So-net光 NUW)** のレコード一覧
2. 自動入力したい顧客のレコードを開く
3. レコード詳細画面の上部にある **青いボタン「So-net 申込実行」** をクリック

### 初回のみのダイアログ

ボタンを初めて押した時、Windows から
**「このリンクで何を開く？」** と聞かれます:

1. リストから **「sonet-rpa」** を選択
2. **「常に使う」** にチェックを入れる
3. **「開く」** ボタンを押す

これで次回からはダイアログが出ずに直接 Firefox が起動します。

### 自動入力の流れ

1. Firefox が立ち上がる
2. So-net 代理店ログイン画面に自動ログイン
3. 9ステップで自動入力（代理店コード→エリア→コース→...→入会情報）
4. 確認画面で **自動入力が停止**
5. 確認画面で内容を目視チェック
6. 問題なければ画面下の **「申込」ボタン** を手動でクリック
7. ブラウザを閉じる → 処理完了

---

## 3. こんな時はどうする？

### Q. 自動入力が途中で止まった
- ブラウザを閉じてください
- kintone レコードの入力内容に不備がないか確認
- もう一度ボタンを押してやり直し

### Q. エラーダイアログが出た
- ダイアログのメッセージを **メモかスクショ** で保存
- 連絡先に問い合わせてください

### Q. ボタンを押しても何も起きない
- 別のレコードでも試してみる
- kintone の画面を再読込（F5）してから再度試す
- 「初回のみのダイアログ」で「sonet-rpa」以外を選んでしまった可能性があります → 連絡先に問い合わせ

### Q. 申込区分が「新設」以外のレコードで使いたい
- 現状 **新設のみ対応**。転用・事業者変更は手動で申込してください

### Q. 申し込みできない条件
- 光電話=なし のときは無線LANカードは選択不可
- kintone 側で矛盾している場合、起動時にエラーダイアログが出ます

### Q. ファイルを別の PC に移したい
- 配布された ZIP を改めて展開 → setup.bat 実行で OK

---

## 4. 注意事項

### セキュリティ
- ZIP には認証情報（So-net ログイン）が含まれています
- 第三者と共有しないでください
- 退職時や PC 廃棄時は、解凍されたフォルダを **完全削除** してください

### 確認画面停止の意味
- このツールは **必ず確認画面で止まります**
- 「申込」ボタンは操作者が **目視確認して手動で押す** 設計です
- 誤申込防止のため、確認画面のスキップ機能は付いていません

### kintone のレコード入力
- 必須項目に欠損があると自動入力エラーになります
- 「申込区分」は必ず「新規入会」にしてください
- 住所・電話番号・郵便番号は正しく入力してください

---

## 5. 連絡先

トラブル・要望は下記までお知らせください:
- メール: （連絡先を記入してください）
- Slack: （連絡先を記入してください）
