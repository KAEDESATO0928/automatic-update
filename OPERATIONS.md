# 運用メモ・未解決事項・改善案

開発者・管理者向けのメモ。配布物には含めない。

最終更新: 2026-05-28

---

## A. 未解決事項（実装が必要）

### A1. 個人/法人の判定
- kintone `文字列__1行__20` (個人/法人) を未使用
- 法人申込は So-net フォームで別フローの可能性（要 So-net フォーム調査）
- **明日 (2026-05-29) 現場確認予定**
- 現状: 個人前提で動作。法人レコードを実行すると入力エラーの可能性

### A2. tkinter ダイアログの設計（C項目、繰越）
- 現状: 起動前エラーのみダイアログ表示、Firefox 起動後は print + ブラウザ放置
- 課題: Firefox 起動後のセレクタ失敗時、操作者が原因を理解しづらい
- 案:
  - 致命的エラー（Playwright TimeoutError 等）のみダイアログ
  - 通知ライブラリ（plyer 等）でトースト通知
  - エラー内容＋スクショパスをまとめて表示
- 結論未定（2026-05-29 以降に検討）

### A3. 10ギガコースのテスト不足
- 10G で Wi-Fi7ルーター単独 (`router_wifi7_10g=〇`, `wireless_lan_10g=×`) のテスト未実施
- 10G で「ご自身で用意する」(両方×) のテスト未実施
- 10G + 光電話=申込なし のテスト未実施

### A4. 申込区分が新設以外のレコード対応
- 設計上スコープ外（転用・事業者変更は未対応）
- 起動時に申込区分チェックで弾く実装済み
- 将来、対象範囲を広げる場合は So-net フォームの転用/事業者変更フローを実装する必要あり

### A5. キャンペーンコード新の活用
- 抽出ロジック実装済みだが、テストレコードには未設定
- 旧フィールド `文字列__1行__36` の値と一致するか実運用で確認必要
- 不一致時の挙動: キャンペーンコード新が空なら旧フィールドにフォールバック（実装済み）

---

## B. 既知の問題・潜在的バグ

### B1. step7 (option_next) の URL ベース判定が不安定
- 当初 URL 変化で「次のページへ進めたか」を判定 → 1G/10G で URL が変わらない時があり誤検知
- 現在は **次ページの特徴的セレクタ (`#UP2010_usrFamilyNameKnj`) で判定**
- 副作用: 次ページのフィールド ID が変わると判定が壊れる

### B2. 住所分割の精度
- `record_mapper.split_address()` は正規表現ベースで best-effort
- 想定外の住所表記（漢数字、独特の地番表記）で失敗の可能性
- 失敗時は分割せずそのまま So-net 入力 → 確認画面で目視修正前提
- 改善: ジオコーディング API 利用、または手動で再分割する UI

### B3. 10ギガコース時の中間ページ (UISST0146)
- UISST0070 で「決定」を押すと UISST0146 という同じ form id の確認ページに遷移
- 現状 step5b で「最大2回まで決定リトライ」で対応
- 副作用: 万一 3回目の確認画面が増えると詰まる

### B4. 郵便番号と住所の不一致
- 田中太郎のテストデータで郵便番号 `904-2164`（沖縄市）に対し住所「沖縄県浦添市」を入れたが、So-net 側は郵便番号優先で「沖縄市」と判定して入力
- kintone 側で郵便番号と住所が不整合の場合、So-net 側でジオコーディング結果が住所と異なる可能性
- ユーザー側で kintone データのバリデーションを強化するか、警告を入れる

### B5. オプション処理順序の依存
- step6 内で 光電話 → 光テレビ → ルーター → シンプル9個 の順で処理
- 順序を変えると So-net 側のカート状態が変わる可能性（未検証）

### B6. 無線LANカードと光電話の依存
- 光電話=なし + 無線LAN=〇 → バリデーションエラー（実装済み）
- 逆: 光電話=あり + 無線LAN=× は問題なし

### B7. 安心サポートプラスのバンドル
- ASPP を選ぶと So-net 側で「安心サポート」「S-SAFE ID Keeper」が自動的にバンドルされる
- kintone 側で「安心サポートプラス=〇 + S-SAFE=×」のような組み合わせの挙動は未検証

### B8. ブラウザクラッシュ時の挙動
- Playwright の Firefox がクラッシュした場合、main.py は例外で終了
- セッションは失われる（kintone への書き戻しもないので問題ないが、ログには記録される）

### B9. 多重起動
- 同じレコードで複数回ボタンを押した場合、複数 Firefox プロセスが立ち上がる
- ロックファイルなどの多重起動防止策は未実装

---

## C. 改善できる部分

### C1. ログの構造化
- 現状 print のみ。logs/ ディレクトリは未活用
- 構造化ログ（JSON）にすると後で問題切り分けが楽
- 改善案: `logging` モジュール + ローテート

### C2. 設定の外出し
- COURSE_MAP, OPTION_INDEX_MAP, PHONE_SERVICE_MAP 等をハードコード
- YAML/TOML に外出しすれば、So-net 仕様変更時に再ビルド不要

### C3. PyInstaller .exe 化
- 現状 Python + pip install + playwright install firefox が必要
- .exe 化すれば1ファイル配布で済むがサイズ500MB+ → 配布の手間と容量のトレードオフ

### C4. setup.bat の堅牢化
- Python なし時に Microsoft Store URL を開くだけ → ユーザーが Python 3.13 ではなく 3.12 や 3.14 を選ぶ可能性
- 案: バージョンチェックを追加 → 不一致なら明示的に Python 3.13 をダウンロード

### C5. .env の安全配布
- 現状 ZIP に直接同梱 → ZIP 自体の管理が必要
- 改善案:
  - 認証情報を暗号化して同梱、setup.bat でパスワード復号
  - 別ファイルで認証情報のみ配布
  - kintone の管理者画面で API トークン管理

### C6. URL スキーム登録の堅牢化
- setup.bat で動的に `.reg` を生成して `reg import`
- 副作用: 既存登録を強制上書き
- 改善案: 既存登録があれば確認ダイアログ表示

### C7. So-net のフォーム変更への追従性
- main.py 内のセレクタ ID (`#UP1360_agentCd` 等) がハードコード
- So-net が UI 改修すると壊れる
- 改善案: セレクタを定数化、E2E テストで早期検知

### C8. 自動更新機能
- 配布した PC のコードを後から更新する手段がない
- 改善案: git pull、または S3 から最新版をダウンロードする更新スクリプト

### C9. UI フィードバック
- 自動入力中、操作者はターミナルか Firefox しか見えない
- 進捗を示すウィンドウ（tkinter）を出すと「動いてるな」と分かりやすい

### C10. 住所分割の改善
- B2 と関連
- ジオコーディング API (国土地理院, Yahoo) で精度向上できる
- ただし API キー管理の負担が増える

---

## D. 運用上の注意

### D1. kintone API トークンの管理
- 現在テスト用トークン（read-only）使用
- 本番運用では app 68 (本番アプリ) のトークンを発行し、 .env に設定
- トークンに「レコード閲覧」以外の権限を付与しない（書き込み不要）

### D2. So-net 認証情報の管理
- 共有アカウント (nu_t-assist) 利用
- 個別アカウントに切り替える場合は、各操作者の .env を個別配布する必要あり

### D3. URL スキーム衝突
- `apclo-sonet://` は社名 prefix で衝突回避
- 別の RPA を追加する場合は別のスキーム名（例: `apclo-nuro://`）にする

### D4. ファイル配置
- setup.bat は **自分のディレクトリを基準** に .reg を動的生成
- 操作者がフォルダを移動すると URL スキームが壊れる → setup.bat 再実行が必要

### D5. ブラウザ自動更新
- Playwright Firefox は固定バージョン
- `playwright install firefox --force` で更新可能
- 数ヶ月に1回程度の更新を推奨

---

## E. 拡張案（将来）

### E1. 他キャリアへの展開
- 同じ仕組みで NURO光、auひかり、ドコモ光 等の RPA を作成可能
- URL スキームを `apclo-nuro://` `apclo-auhikari://` 等に分けて多用化

### E2. 他システム連携
- 申込完了後の結果を kintone に書き戻し（現状は書き戻しなし）
- Slack 通知（処理結果を社内チャンネルに自動投稿）

### E3. バッチ実行
- 複数顧客を連続申込（夜間バッチ等）
- ただし So-net 側がレート制限する可能性

### E4. 機械学習による住所分割
- B2/C10 と関連
- 過去の住所データで学習したモデルで分割精度向上

### E5. クラウド実行
- 現状 ローカル PC 必須（VPS 不可の制約）
- 制約緩和されれば、AWS Lambda + Playwright で実行可能

---

## F. 未テスト・要検証項目（実運用時に注意）

- [ ] 法人申込
- [ ] 東日本コース（鈴木花子で 10G_東日本 のみテスト済み、M_東日本 は未検証）
- [ ] 10G コースで Wi-Fi7 ルーター単独
- [ ] 10G コースで「ご自身で用意」
- [ ] 集合住宅・分譲のテスト（佐藤健で 10G_マンション_東日本 で実施済み、1G は未検証）
- [ ] 住所が複雑なケース（沖縄の字名、京都の通り名表記、漢数字）
- [ ] 郵便番号と住所が不整合なケース
- [ ] 多重起動時の挙動
- [ ] kintone API レート制限
- [ ] So-net 側の同時セッション制限

---

## G. 開発時のメモ

### Phase 進捗
- Phase 1: シンプル9オプション ✓
- Phase 2: 光電話 ✓
- Phase 3: 光テレビ ✓
- Phase 4: ルーター + 無線LAN ✓
- Phase 5: 10G 対応 ✓
- Phase 6: 東西判定 ✓
- Phase 7: キャンペーンコード ✓
- Phase 8: 矛盾バリデーション ✓
- 配布パッケージ ✓
- セットアップスクリプト ✓
- ドキュメント ✓

### Git
- Repo: https://github.com/KAEDESATO0928/automatic-update
- Visibility: **PUBLIC** ← 認証情報を直接 push しないよう注意
- .env と experiment_*.py は .gitignore で除外済み

### テスト環境
- kintone: vegb5vs2uvvz.cybozu.com (app_id=178 はテスト用)
- 本番アプリは app_id=68 (So-net光 NUW)
- テストレコード: 2817 (田中太郎), 2818 (鈴木花子), 2819 (佐藤健), 2820 (山田美咲)

### kintone JS カスタマイズ
- `sonet-rpa.js` を app 178 に登録済み
- 本番展開時は app 68 にも同じ JS を登録する必要あり
